### Interactive[LEAGUE].ipynb 

These are interactive notebooks which allows user to:

- Relocate teams by dragging the marker on the map.
- Change team attributes by clicking a marker.
- Create new team by right clicking on the map.
- Re-calcuating the heat map.

    - Everytime this is done a table is generated comparing the new heat map numbers to the prior (initial) state.
    - If the Total

Not implemented yet:

- Ability to change the league's "weight" indicating the popularity of the sport and league.
- Ability to make a later point as the prior heat map numbers instead of on the initial render.